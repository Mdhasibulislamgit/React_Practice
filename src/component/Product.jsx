const Product = (Product) => {
  return (
    <div>
      <h1>Product</h1>
      <p>{Product.product.name}</p>
      <p>{Product.product.price}</p>
    </div>
  );
};
export default Product;
